fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
Pillow==10.4.0
piexif==1.1.3
anthropic>=0.34.0
pysmb==1.2.9.1
paramiko==3.4.0
python-dotenv==1.0.1

# ── Optional: local CLIP pre-filtering (reduces Claude API calls ~33%) ──
# Uncomment to enable hybrid local+Claude scoring.
# First run downloads the CLIP model (~340 MB) to ~/.cache/huggingface/
#
# torch>=2.0.0
# transformers>=4.35.0
